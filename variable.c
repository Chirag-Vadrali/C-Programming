/* variable scope referes to where a variable is recognizedand accessible.
variables can share the name if theyre in differnet scopes{}
and also declaring in global is not a good practice and is hard to debug. */